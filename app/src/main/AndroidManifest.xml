<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed for network requests -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.glpred"
        >

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EnterDataActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".ConfirmDataActivity" android:parentActivityName=".EnterDataActivity" />
        <activity android:name=".FetchDatabaseActivity" android:parentActivityName=".MainActivity" />

    </application>

</manifest>
